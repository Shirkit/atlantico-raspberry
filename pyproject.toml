[project]
name = "atlantico_rpi"
version = "0.0.0"
description = "Atlantico Raspberry Pi device package"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = { find = { include = ["atlantico_rpi"] } }
